# Extract the pico.sh username from a peer identifier

Extract the pico.sh username from a peer identifier

## Usage

``` r
pico_peer_username(peer_id)
```

## Arguments

- peer_id:

  (character string) A peer identifier of format
  `{username}@{hostname}:{pid}` or just `{username}`.

## Value

A character string.
